package Gamemech;

public class Character
{
	//###################################
	//				Geisterfressbar
	
	// System.out.println(zeit);
	// //beendet den TiltedZustand
	// 
	// System.out.println(u.x+" ; "+x+" "+u.y+" ; "+y);
	//#########################################
	//					Bigcoins
	//  //abschalten des TiltedZustandes
	// } else 
	//#############################################
	//					Richtungsabfrage#
	// private boolean getPossible(BeweglichesObjekt u) {
	//
	// int xrichtung = u.getXrichtung();
	// int yrichtung = u.getYrichtung();
	// int l = 14;
	//
	// if (xrichtung < 0) { // bewegung nach links
	// return && u.getObjektBei(x, y + l) != null;
	// } else if (xrichtung > 0) { // bewegung nach rechts
	// return u.getObjektBei(x + l, y) == null && u.getObjektBei(x + l, y + l) !=
	// null;
	//
	// } else if (yrichtung < 0) { // bewegung nach oben
	// return u.getObjektBei(x, y) == null && u.getObjektBei(x + l, y) != null;
	//
	// } else {// bewegung nach unten
	// return( u.getObjektBei(x, y + l) == null && u.getObjektBei(x + l, y + l) !=
	// null);
	//
	// }
	//
	// // && u.getObjektBei(x+16,y) == null &&
	// // u.getObjektBei(x,y+16) == null && u.getObjektBei(x+16,y+16) == null
	// // ;
	//
	// }
	
}
